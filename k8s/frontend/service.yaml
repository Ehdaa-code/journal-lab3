# frontend service.yaml
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: journal
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP