# image-service service.yaml
apiVersion: v1
kind: Service
metadata:
  name: image-service
  namespace: journal
spec:
  selector:
    app: image-service
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP