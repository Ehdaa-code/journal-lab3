# search-service service.yaml
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: journal
spec:
  selector:
    app: search-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP